#include <windows.h>

int WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPSTR pCmdLine, int nCmdShow) {
    MessageBoxA(NULL, "This is a test application", "Message", MB_OK);

    return 0;
}